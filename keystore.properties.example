# This is an example keystore.properties file for local builds.
# Copy this file to keystore.properties and fill in your values.
# Note: keystore.properties is gitignored for security.

# For local builds, you can use this file:
KEYSTORE_FILE=/path/to/your.keystore
KEY_ALIAS=your_alias
KEY_PASSWORD=your_key_password
KEYSTORE_PASSWORD=your_keystore_password

# For GitHub Actions, these are set as environment variables from secrets.
# See .github/README.md for setup instructions.

